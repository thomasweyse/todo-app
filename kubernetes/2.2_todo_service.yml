apiVersion: v1
kind: Service
metadata:
  labels:
    app: todo-app
  name: todo-svc
  namespace: todo-app-xx
spec:
  type: NodePort
  ports:
  - name: todo-svc
    port: 3000
    targetPort: 3000
    nodePort: 310xx
  selector:
    app: todo-app
  